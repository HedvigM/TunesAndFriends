import { styled } from "@mui/material";

export const TuneInfo = () => {
  return (
    <OuterContainer>
      <TuneType>reel</TuneType>
      <FriendsList>Jorid</FriendsList>
    </OuterContainer>
  );
};

const OuterContainer = styled("div")``;
const TuneType = styled("div")``;
const FriendsList = styled("div")``;
